name: textformfield_unit_example
description: Demonstrates how to use the textformfield_unit plugin.
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.3.0"

dependencies:
  flutter:
    sdk: flutter

  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  textformfield_unit: ^1.0.6
  flutter_lints: ^5.0.0

dependency_overrides:
  textformfield_unit:
    path: ../

flutter:
  uses-material-design: true