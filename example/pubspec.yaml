name: textformfield_unit_example
description: Demonstrates how to use the textformfield_unit plugin.
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.3.0"

dependencies:
  flutter:
    sdk: flutter

  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter

  textformfield_unit: ^1.0.8
  flutter_lints: ^6.0.0
  material_ui: ^1.2.0

dependency_overrides:
  textformfield_unit:
    path: ../

flutter:
  uses-material-design: true